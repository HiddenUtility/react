# Node.jsをインストールする。
奇数バージョンが安定

# 確認
```command
node --version
```
npmをアップグレード
```command
npm install -g npm
```

# Reactのプロジェクトを作る
```command
cd <pjt作成するところ>
npx create-react-app <pjt名>
```
もしくは
```command
cd <pjt作成するところ>
npm init react-app <pjt名>
```